# תיקונים לביצוע אחרי הקמפיין

נכתב ב-01/09/2026, אחרי אירוע ההשהיות של 31/8–1/9.

המסמך מרכז את מה שנמצא ולא בוצע, כי הסיכון לבצע אותו תוך כדי קמפיין חי גדול מהתועלת. כל סעיף כולל את הראיה שעליה הוא מבוסס, כדי שאפשר יהיה לאמת אותו מחדש ולא לסמוך על הזיכרון של המסמך הזה.

## מה כן בוצע ונפרס

| קומיט | שינוי | נמדד |
| --- | --- | --- |
| `1b469c1` | שמירת פריטי inbox: 5000/24h → 300/2h | 7.9s → 1.2s ל-100 הודעות |
| `5e5db25` | הסרת עותק תסריט הקמפיין ממצבי שיחה | 13MB → 866KB |
| `46e40db` | מעקב ברמת שורה ל-`conversation_state` | 20.6ms → 0.20ms ב-1,200 שיחות |
| `53e3254` | קיצור backoff בגייטווי + ניסיון חוזר מיידי | 3 פספוסים: 35s → 3.5s |

---

## עדיפות 1 — מוכן, נבדק, מחכה לפריסה

### 1.1 שכפול חלקי של הסנפשוט הנשמר

**מצב:** מוכן על הענף `perf/clone-snapshot-tables` (קומיט `1696fc0`). לא על master.

הסנפשוט הנשמר חייב להיות עותק קפוא כדי שהכתיבה הבאה תוכל להשוות מולו. שכפול כל ה-`StorageData` בכל מחזור נמדד ב-98ms חסימת event loop. `cloneSnapshotForTables` משכפל רק את הטבלאות שהכתיבה נגעה בהן; טבלה שלא נגעו בה מחזיקה את העותק מהפעם שהיא כן השתנתה, שזה בדיוק מה שיושב ב-PostgreSQL.

נמדד על 13k outbox / 18k events / 13k results:

| מסלול | לפני | אחרי |
| --- | --- | --- |
| שליחת הודעה | 256ms | 71ms |
| שינוי מצב שיחה | 256ms | 5ms |
| אירוע קמפיין + תוצאה | 256ms | 105ms |

**בדיקות:** `scripts/test-postgres-no-lost-writes.js` (על הענף) בונה DB צללית מה-SQL שהבקאנד הנפיק ומשווה מול הזיכרון טבלה-טבלה. אומת שהיא תופסת באגים — שינוי מכוון של השכפול לשאת הפניות חיות מפיל אותה על ההנחה הראשונה.

**למה נדחה:** זה נוגע בשכבת ההתמדה, בניגוד לתיקון ה-backoff שנוגע רק בתזמון. הסיכון גבוה יותר והתשואה על ההשהייה שהמשתמש מרגיש נמוכה יותר.

**איך לפרוס:** `git merge perf/clone-snapshot-tables` ל-master, להריץ את הסוויטה, לפרוס ללקוחה בלבד.

---

## עדיפות 2 — צווארי בקבוק שנשארו

### 2.1 `campaignEvents` ו-`campaignResults` הן הטבלאות הגדולות

גם אחרי 1.1, המסלול של אירוע קמפיין + תוצאה עדיין עולה 105ms. אלו שתי הטבלאות הצומחות ביותר (18k ו-13k), והן נכתבות בכל טריגר — כלומר בדיוק בשיא העומס.

כיוונים אפשריים, לפי סדר:
1. מדיניות שמירה ל-`campaignEvents` (הם לוג, לא מצב).
2. מסלול append-only — אירועים כמעט אף פעם לא מתעדכנים, רק נוספים.
3. מעקב ברמת שורה גם לשתיהן, כמו שנעשה ל-`conversation_state`.

### 2.2 שמירת מצבי שיחה פגי תוקף ל-24 שעות

`FLOW_RECOVERY_WINDOW_MS` שומר מצב `expired-decision` 24 שעות כדי לאפשר חידוש של תשובה מאוחרת (`TIMED_OUT_REPLY_RESUMED`). זו הסיבה שנמדדו 1,075 מצבי `expired-decision` מול 144 `decision` פעילים.

זו התנהגות מכוונת עם ערך מוצרי אמיתי, אז זה לא באג — אבל שווה להחליט אם 24 שעות זה הערך הנכון, או שחלון קצר יותר נותן את אותה תועלת ברבע מהנפח.

### 2.3 ה-timeout של 3 שניות לשאילתת הניתוב

`AbortSignal.timeout(3_000)` בשאילתת `meta-routing-snapshot`. עכשיו יש ניסיון חוזר מיידי, אז פספוס בודד כבר לא יקר. אבל שווה למדוד כמה זמן הלקוח באמת לוקח בשיא עומס לפני שמחליטים אם 3 שניות זה הערך הנכון.

---

## עדיפות 3 — תשתית ופריסה

### 3.1 אין מטפל `SIGTERM` בשום מקום

מאומת: אפס תוצאות ל-`SIGTERM`/`SIGINT` בכל `src/*.ts`. יש רק `unhandledRejection`.

המשמעות: כל פריסה הורגת את התהליך בלי לרוקן כתיבות ממתינות. עם כתיבות מקובצות ל-PostgreSQL, זה חלון אמיתי לאיבוד מידע בדיוק ברגע הפריסה. זה כנראה הפריט הכי חשוב ברשימה הזו.

### 3.2 אין `HEALTHCHECK` ב-Dockerfile

מאומת: אפס תוצאות. בלי זה, Dokploy לא יודע להבדיל בין קונטיינר חי לקונטיינר שה-event loop שלו חסום — וזה בדיוק המצב שייצר את שגיאות ה-502 בלוג הגייטווי.

### 3.3 `new Pool` בלי הגדרות

מאומת: 4 מופעים של `new Pool({ connectionString: databaseUrl })` ב-[src/database.ts](src/database.ts), בלי `max`, בלי `connectionTimeoutMillis`, בלי `idleTimeoutMillis`. ברירות המחדל של `pg` לא בהכרח מתאימות לעומס קמפיין.

### 3.4 Chromium מותקן תמיד ולא בשימוש

מאומת: 4 אזכורים ב-Dockerfile. `BAILEYS_FALLBACK_TO_WEBJS=false` אצל כל הלקוחות, כלומר הנתיב הזה לא ניתן להגעה. עלות: כ-128 שניות בנייה ו-759MB לתמונה.

### 3.5 `npm ci` רץ פעמיים

מאומת: 2 מופעים ב-Dockerfile. כ-210 שניות. יחד עם 3.4, זמן הבנייה יכול לרדת משמעותית.

### 3.6 `pruneCompleted` לא מנקה `failed`

ב-[src/metaGatewayInbox.ts:124](src/metaGatewayInbox.ts:124) הסינון הוא `item.status !== 'completed'`, כלומר פריטי `failed` נשמרים לנצח. נמצאו 76 פריטים תקועים מ-27–28/8.

---

## עדיפות 4 — היגיינה

### 4.1 סוויטת הבדיקות

- ארבע בדיקות נוגעות ב-PostgreSQL אמיתי ומתנגשות זו בזו במקביליות (deadlock, "already contains a different storage snapshot"). צריך DB מבודד לכל בדיקה, או להריץ אותן סדרתית מתוך סקריפט.
- `test-postgres-migration-pilot` ו-`test-migration-safety` דורשות DB פיילוט **ריק** ונכשלות אחרי שבדיקות אחרות מילאו אותו. אומת מול baseline שזה קדם לשינויים.
- `test-referral-ranking`, `test-load-burst-todays-launch`, `test-email-export` מדפיסות "tests passed" ואז נתקעות ב-teardown (exit 124/127, handle פתוח). ההנחות עוברות; התהליך לא יוצא.
- אין `npm test` שמריץ את כולן. שווה להוסיף, עם הפרדה בין בדיקות שדורשות DB לכאלה שלא.

### 4.2 תלויות

- 17 פגיעויות ב-`npm audit`.
- Baileys מוצמד לגרסת `rc`.

---

## הערה על סדר הפעולות

הלקח מהאירוע הזה: ההשהייה שהמשתמש מרגיש נחלקת לשני דברים נפרדים, וקל לבלבל ביניהם.

- **הסתברות** לפספוס — כמה פעמים הלקוח לא מספיק לענות בחלון. שם יושבים תיקוני 1.1 ו-2.1.
- **מחיר** של פספוס — מה קורה כשהוא לא מספיק. שם ישבו 50 השניות, ושם ישב `53e3254`.

רוב העבודה הראשונית הושקעה בצד ההסתברות, בזמן שכל הזמן שנמדד בפועל ישב בצד המחיר. כשעולה שוב דיווח על השהייה, כדאי קודם לפרק את המספר לרכיבים ולראות באיזה צד הוא נופל.
