# סבילות מדידה — נקבעה לפני הרצת ה-baseline (2026-09-20)

נקבעה ותועדה **לפני** כל ריצת מדידה, ואינה משתנה אחרי שראו תוצאות.

- HEAD בסיס: `0ea8d26`, קוד `src/` ללא שינוי בזמן ה-baseline.
- מתודולוגיה: warm-up אחד (נזרק) + 5 ריצות מדודות לכל תרחיש; כל ריצה בתהליך Node נקי; ברצף בלבד.
- קבוצות נמדדות בנפרד: focus, small-a, small-b (Meta), baileys.
- סבילות (חציון-של-חציונים ו-p95-של-p95 על פני 5 הריצות, לכל קבוצה):
  - חציון: עד +10% מה-baseline
  - p95: עד +15% מה-baseline
  - p99 ו-max: מדווחים ואינם שער, אך נסיגה חדה (>+25%) מסומנת לבירור
- רעש מדידה: מדווח כטווח min–max בין 5 הריצות; אם טווח ה-baseline עצמו רחב מהסבילות, תוצאה בתוך הטווח אינה נחשבת נסיגה מובהקת, ויצוין במפורש.
- סף 7 שניות מוחלט: תצפית בלבד, לא שער קבלה.
- כלי המדידה: `scripts/test-load-shared-campaign-isolation.js` (הורחב בשורת RESULT_JSON לכל קבוצה + p99; שינוי בדיווח בלבד, לא בלוגיקה), `scripts/run-load-baseline.js`.
