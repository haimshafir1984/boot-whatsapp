# 🎯 UX Review — דשבורד לקוחה FlowsBiz

*(client-test-c09aec1c.flowsbiz.com/client/ — נראות, פרטים, וזרימת "הוסף קמפיין")*

## ציון כללי: 6/10

יש כאן בסיס טוב באמת: אשף יצירת קמפיין מחולק ל-4 שלבים ברורים, סטפר עם מספור, ו**תצוגת שיחה חיה** בצד — מהלך מצוין שמראה ללקוחה איך ההודעות ייראו. מה שמוריד את הציון הוא שהדשבורד **חושף ללקוחה שכבה טכנית שלמה שלא נועדה לה** (הגדרות Twilio, אישור טמפלטים, מונחי API באנגלית), ששלב 3 באשף עמוס מאוד, וכמה בעיות ניגודיות וקריאות. הקהל שלך — לקוחות לרוב לא-טכניות — ייבהל או יתבלבל מהרבה ממה שמוצג כרגע. רוב הבעיות הן הסתרה/ניסוח, לא בנייה מחדש, ולכן זול לתקן.

**נבדק:** דשבורד ראשי (גלילה מלאה), אשף "הוסף קמפיין" שלבים 1-4 כולל הוספת שאלת בחירה מסוג "כפתורים" עם 3 תשובות, ומסך הסיום עם הלינק. נבדק גם קוד ה-CSS לרספונסיביות. · **מטרת משתמש מונחת:** לקוחה לא-טכנית מקימה בעצמה קמפיין בוט אחד ומקבלת לינק לשיתוף.

---

## ממצאים

### 🔴 קריטי — לתקן לפני הכול

- **הדשבורד חושף ללקוחה הגדרות טכניות של Twilio/Meta.** *הבעיה:* כרטיס "קישור WHATSAPP / TWILIO" מציג ללקוחה ערכים כמו `TWILIO_API`, "Messaging Service: לא מוגדר", "מצב עבודה", וצ'יפים כמו "✓ מצב Twilio". מתחתיו טופס onboarding מלא (שם מותג, אתר, תיאור opt-in) וגם אזור "טמפלטים לאישור WHATSAPP" עם שדות באנגלית (`first_real_estate_followup`, `MARKETING`, `he`) ומשתני `{{1}}`. *השפעה:* ללקוחה לא-טכנית (ובמיוחד בקהל היעד שלך) זה ג'יבריש מאיים — זה משדר "המערכת מסובכת, אני אשבור משהו", פוגע באמון ומגדיל פניות תמיכה. זה גם מידע ברמת בעל-מערכת שלא אמור לחיות בכלל בדשבורד הלקוחה. *תיקון:* להסתיר את כל הבלוקים האלה מתצוגת הלקוחה (render מותנה לפי תפקיד), או למזער ל-collapse אחד בשם "הגדרות מתקדמות" שסגור כברירת מחדל. את ניהול הטמפלטים להעביר לדשבורד המנהל.

- **מונחים וערכים באנגלית בתוך ממשק עברי ללקוחה.** *הבעיה:* `TWILIO_API`, `MARKETING`, `he`, `first_real_estate_followup`, "Messaging Service" מופיעים בתוך מסך עברי-RTL. *השפעה:* שובר את תחושת ה"מוצר בעברית", מקטין הבנה, ומבליט שזו תשתית מפתחים. *תיקון:* לתרגם/להחביא כל מחרוזת שפונה ללקוחה. אם ערך חייב להישאר טכני, שלא יוצג ללקוחה בכלל.

### 🟡 בינוני — לתקן בקרוב

- **שלב 3 באשף עמוס מדי.** *הבעיה:* "שלב 1" בודד מכיל: דרופדאון סוג שאלה, דרופדאון סוג הצגה (כפתורים/רשימה/טקסט ממוספר), העלאת קובץ, בלוק timeout שלם (דקות + קובץ + "שלח כמדבקה" + הודעת timeout), ועד 3 תשובות — שלכל אחת יש שדה טקסט, **שלושה** כפתורי פעולה (שלח הודעה וסיים / שלח קובץ / המשך לשלב הבא) ו"הודעת פרידה קבועה". *השפעה:* עומס קוגניטיבי גבוה, טופס ארוך מאוד וחזרתי, קל ללכת לאיבוד או לוותר. *תיקון:* להסתיר את בלוק ה-timeout ואת "הודעת פרידה" מאחורי "אפשרויות מתקדמות" מתקפל; להציג את שלושת כפתורי הפעולה כדרופדאון אחד ("מה לעשות אחרי תשובה זו?") במקום שלושה כפתורים בכל תשובה.

- **התצוגה החיה לא מתעדכנת בזמן בניית השאלות (שלב 3).** *הבעיה:* תצוגת השיחה מצד שמאל מציגה את הטריגר, שאלת השם והודעת הסיום — אבל **לא** את שאלות הבחירה שאני בונה בשלב 3. הן מופיעות רק במסך הסיכום (שלב 4). *השפעה:* בדיוק בשלב הכי מורכב אין משוב חזותי, והלקוחה לא רואה מה היא בונה עד הסוף. *תיקון:* מאחר שהקוד כבר יודע לרנדר את השאלות בשלב 4 — להריץ את אותו רינדור גם בשלב 3 בזמן אמת.

- **ניגודיות נמוכה בכפתורי הפעולה ובטקסט משני.** *הבעיה:* כפתורי "שלח קובץ" / "המשך לשלב הבא" וטקסטי עזר/תוויות אפורים-בהירים נראים דהויים על רקע לבן/ירוק-בהיר. *השפעה:* קשה לקרוא, ובמיוחד מקשה על משתמשים מבוגרים או בתאורה חלשה — סיכון לאי-עמידה ב-WCAG AA (4.5:1 לטקסט רגיל). *תיקון:* להכהות את הטקסט המשני והכפתורים המשניים עד לעמידה ב-4.5:1; לוודא ניגוד ≥3:1 לרכיבי ממשק.

- **הלינק מוצג מקודד ולא קריא.** *הבעיה:* במסך הסיכום הלינק מופיע כ-`https://wa.me/18027218302?text=%D7%94%D7%A6...`. *השפעה:* נראה שבור/חשוד ללקוחה, שלא מבינה את ה-%-encoding. *תיקון:* להציג טקסט ידידותי ("לינק מוכן לשיתוף") עם כפתור "העתק" בולט, ולהסתיר את ה-URL הגולמי (כפתורי ההעתקה/פתיחה במסך ההצלחה — טובים, רק להקדים אותם).

### 🟢 שיפור קל — ליטוש

- **כפתור אדום "🔒 החלף חשבון / התנתק" צמוד לכפתור תפעולי שגרתי.** *הבעיה:* אדום = סכנה/ניתוק, צמוד ל"כבה עד קמפיין פעיל" הכתום. *השפעה:* סיכון ללחיצה לא מכוונת על ניתוק. *תיקון:* להרחיק/למשנֵה משקל את כפתור הניתוק, ולהוסיף אישור לפני ניתוק.

- **אפשרי כפל-הוספה של תשובות.** *הבעיה:* לחיצה אחת על "+ הוסף תשובה" כשהיתה תשובה אחת קפצה לשלוש תשובות. *השפעה:* בלבול קל; ייתכן גם שאפשר לעבור את מגבלת 3 התשובות של כפתורי WhatsApp. *תיקון:* לוודא שהכפתור מוסיף תשובה אחת ושהוא מושבת/מוסתר כשמגיעים ל-3 (במצב "כפתורים").

- **רווח לבן גדול בצד אחד בחלק מהכרטיסים בדסקטופ.** *הבעיה:* כרטיס הסטטוס העליון צר וממורכז עם שוליים ריקים רחבים. *השפעה:* מראה לא-מנוצל, פחות "מוצרי". *תיקון:* ליישר רוחב כרטיסים לעקביות.

## ⭐ Top 3 — הכי הרבה תועלת, הכי מעט מאמץ

1. **להסתיר מהלקוחה את שכבת ה-Twilio/טמפלטים.** הניצחון הגדול בנראות ובאמון, ועלות נמוכה (render מותנה). הלקוחה צריכה לראות: חיבור WhatsApp, אנשי קשר, קמפיינים — ולא `TWILIO_API` ו-`MARKETING`.
2. **לרנדר את התצוגה החיה גם בשלב 3.** הקוד כבר עושה זאת בשלב 4; להפעיל אותו רציף. סוגר את פער המשוב בשלב הכי מבלבל.
3. **להעלות ניגודיות של טקסט וכפתורים משניים.** שינוי CSS קטן, השפעה רוחבית על קריאות ונגישות.

## 🏆 השוואה קצרה למובילים

מול בוני-זרימה כמו **ManyChat** ו-**Wati**: הם משתמשים ב-canvas ויזואלי של "צמתים" שחזק אך מאיים על לא-טכניים; האשף הליניארי שלך **פשוט יותר ומתאים יותר** לקהל שלך — זה יתרון אמיתי, אל תוותר עליו. אבל הם **מסתירים** את כל ה-plumbing הטכני שאתה חושף: לקוח ב-Wati לא רואה לעולם "Messaging SID" או מבנה טמפלט גולמי. מבחינת בהירות טופס, **Tally/Typeform** הם רף טוב: שדה אחד-במוקד, אפשרויות מתקדמות מקופלות, אפס ז'רגון. הדפוס ששווה לאמץ: **progressive disclosure** — להראות את המינימום, ולפתוח "מתקדם" רק למי שביקש.

## ❓ שווה לאמת

- **רספונסיביות במובייל:** בקוד יש `viewport` תקין ו-breakpoint יחיד ב-820px שמאפס את רוב הגרידים הדו-טוריים ל-טור אחד (כולל אשף הקמפיין) — זה תקין. אבל אין breakpoint עדין יותר ל-~390px, וכמה גרידים פנימיים (שורת שלושת כפתורי הפעולה `repeat(3)`, גריד אפשרויות ההחלטה) לא מאופסים ועלולים להצטופף/לגלוש בטלפון צר. כדאי לבדוק על מכשיר אמיתי — כלי הצילום כאן לכד תמיד ברוחב דסקטופ ולא איפשר אימות חזותי במובייל.
- **ניגודיות מדויקת:** ההערכות לעיל מבוססות על צילומי מסך; שווה למדוד את ערכי הצבע האמיתיים מול ספי WCAG.
- **כפל-הוספת תשובות:** לאשש אם זו התנהגות עקבית או צירוף מקרים של לחיצה כפולה.

---
*נוצר עם סקיל ux-reviewer. קמפיין הבדיקה "בדיקת UX" אכן נשמר במערכת — אפשר למחוק אותו מהדשבורד.*
