# AGENTS.md

מסמך הסבר מערכת למפתח/סוכן שעובד על פרויקט FlowsBiz / WhatsApp Status Bot.

המטרה של המסמך היא לתת תמונה ברורה של מה המערכת עושה היום, איך היא בנויה, איך מתפעלים לקוחות וקמפיינים, ומה חשוב לדעת לפני שינוי בקוד.

## תקציר המערכת

המערכת מאפשרת לבנות קמפיינים ב-WhatsApp עבור לקוחות.

במודל הבסיסי, לקוחה מתחברת לדף ניהול אישי, מחברת WhatsApp, בונה קמפיין, מקבלת לינק `wa.me`, ומשתמש קצה ששולח את משפט הטריגר מקבל מענה אוטומטי.

במודל Twilio, הלקוחה לא חייבת לחבר WhatsApp אישי. לכל לקוחה אפשר להגדיר מספר WhatsApp דרך Twilio, וכל ההודעות נכנסות דרך Webhook ליחידה המבודדת שלה.

המערכת יודעת:

1. לנהל לקוחות מבודדים דרך דשבורד בעלים.
2. ליצור לכל לקוחה יחידה נפרדת ב-Dokploy.
3. להפעיל לקוחות במסלול WhatsApp Web / Baileys או במסלול Twilio API.
4. לבנות קמפיינים עם משפט טריגר ולינק שיתוף.
5. לשאול שם לשמירה, אם האפשרות פעילה.
6. לשמור או לעדכן אנשי קשר ב-Google Contacts או לנהל תור שמירה מקומי.
7. לשלוח הודעת סיום, הודעות המשך, שאלות בחירה, קבצים ותשובות לפי נתיב.
8. לנהל "מעבר לשאלה הבאה" בצורה טבעית: אם יש שאלה נוספת עוברים אליה, ואם אין שאלה נוספת מסיימים.
9. לשלוח הודעת מעבר לנציג אנושי כשמשתמש שואל שאלה שלא קשורה לבחירות הקמפיין.
10. להשאיר חלון מעבר לנציג פתוח ל-24 שעות אחרי סיום הקמפיין.
11. להציג סטטיסטיקות תפעוליות, משפך קמפיין ויומן פעולות פשוט ללקוחות Twilio.

## פריסה וסביבות

המערכת נפרסת מתוך הריפו:

```text
haimshafir1984/boot-whatsapp
```

הענף הפעיל לפריסה:

```text
master
```

הפריסה מנוהלת דרך Dokploy.

דומיינים עיקריים:

- אתר ציבורי: `https://flowsbiz.com/`
- דשבורד בעלים: `https://admin.flowsbiz.com/owner/`
- דפי לקוחות: נוצרים כדומיינים נפרדים תחת `flowsbiz.com`, לפי Dokploy.

כל לקוחה אמורה לקבל:

- אפליקציה נפרדת ב-Dokploy.
- Volume נפרד.
- נתוני קמפיינים נפרדים.
- חיבור Google נפרד.
- קבצים נפרדים.
- הגדרת WhatsApp / Twilio נפרדת.

## פקודות פיתוח

```bash
npm run dev
npm run build
npm start
```

פירוט:

- `npm run dev` מריץ את המערכת דרך `ts-node`.
- `npm run build` מקמפל TypeScript לתיקיית `dist`.
- `npm start` מריץ את הקוד המקומפל.

ב-Windows, אם `npm run build` נחסם בגלל PowerShell execution policy, להשתמש:

```bash
npm.cmd run build
```

אין כרגע סקריפט בדיקות או lint.

## מבנה קבצים חשוב

| נתיב | תפקיד |
| --- | --- |
| `src/adminServer.ts` | שרת Express, API, דשבורד לקוחה, דשבורד בעלים, Webhooks |
| `src/messageFlow.ts` | לוגיקת הודעות WhatsApp, טריגרים, שאלות, handoff, שמירת קשר |
| `src/storage.ts` | מודלי נתונים, קמפיינים, הגדרות, תוצאות ואירועים |
| `src/conversationState.ts` | מצב שיחה זמני בזיכרון: שם, שאלת בחירה, handoff |
| `src/dokployProvisioner.ts` | יצירה ועדכון יחידות לקוח ב-Dokploy |
| `src/ownerStorage.ts` | שמירת לקוחות בדשבורד בעלים |
| `src/providers/TwilioProvider.ts` | שליחה דרך Twilio API |
| `src/twilioEvents.ts` | רישום אירועי Twilio ויומן הודעות |
| `public/index.html` | דשבורד הלקוחה |
| `owner-public/index.html` | דשבורד בעלים ראשי |
| `owner-public/client.html` | דף ניהול לקוחה מתוך דשבורד בעלים |
| `site-public/` | אתר ציבורי |
| `data/contacts.json` | הגדרות, קמפיינים, תוצאות ואנשי קשר של יחידה |
| `owner-data/clients.json` | לקוחות בדשבורד בעלים |

## דשבורד בעלים

דשבורד בעלים נמצא ב:

```text
/owner/
```

דרך הדשבורד אפשר:

- ליצור לקוחה חדשה.
- לבחור מסלול:
  - `basic` - לקוחה רואה בעיקר מצב ותוצאות.
  - `self_service` - לקוחה בונה ומנהלת קמפיינים בעצמה.
  - `advanced` - לקוחה במסלול Twilio.
- להגדיר סיסמת כניסה ללקוחה.
- להגדיר מגבלת קמפיינים.
- להגדיר תוקף שירות.
- להגדיר מספר Twilio ללקוחה.
- להגדיר דיליי בין הודעות אוטומטיות ללקוחה.
- להעתיק Webhook מלא להדבקה ב-Twilio.
- לבנות קמפיין עבור לקוחה מתוך עמוד הלקוחה בדשבורד הבעלים.
- לראות סטטוס WhatsApp, Google וקמפיינים.
- למחוק לקוחה עם אישור כפול.

## מסלולי WhatsApp

### WhatsApp Web / Baileys

במסלול זה הלקוחה מחברת WhatsApp קיים דרך QR או קוד קישור.

זה מתאים לפיילוטים, לקוחות שמפעילים WhatsApp קיים, או מצב שבו הלקוחה רוצה שהשיחות יעברו דרך המספר שלה.

### Twilio API

במסלול זה הלקוחה מקבלת או מחברת מספר WhatsApp דרך Twilio.

הלקוחה לא חייבת לראות את כל ההתכתבות הטכנית. המערכת מציגה לה נתונים תפעוליים במקום יומן Twilio טכני.

הגדרות חשובות ללקוח Twilio:

- `TWILIO_ACCOUNT_SID`
- `TWILIO_AUTH_TOKEN`
- `TWILIO_FROM`
- `TWILIO_WEBHOOK_TOKEN`
- `TWILIO_REQUIRE_SIGNATURE=true`
- `BOT_REPLY_DELAY_MS`

בדשבורד בעלים אפשר להגדיר לכל לקוחה:

- מספר Twilio בפורמט מלא, לדוגמה `+16602902811`.
- דיליי בין הודעות במילישניות.
- Webhook מלא להדבקה ב-Twilio, כולל `token`.

ה-Webhook שצריך להדביק ב-Twilio נמצא בדף הלקוחה בדשבורד בעלים תחת:

```text
Webhook להדבקה בטוויליו
```

יש להדביק אותו ב-Twilio בשדה:

```text
Webhook URL for incoming messages
```

עם:

```text
HTTP POST
```

## דיליי הודעות

הדיליי בין הודעות אוטומטיות נשלט על ידי:

```text
BOT_REPLY_DELAY_MS
```

בדשבורד בעלים יש שדה נוח ללקוח Twilio:

```text
דיליי בין הודעות אוטומטיות
```

דוגמאות:

- `1000` - שנייה אחת.
- `500` - חצי שנייה.
- `0` - ללא המתנה.

ברירת המחדל שמוזרקת ליחידת לקוח דרך Dokploy היא `1000`.

אם לא הוגדר כלום בקוד הריצה עצמו, ברירת המחדל הפנימית הישנה היא `3000`.

## בניית קמפיין

קמפיין כולל:

- שם קמפיין.
- סוג קמפיין:
  - טריגר רגיל.
  - קמפיין ממליץ.
- משפט טריגר.
- זמני פעילות אופציונליים.
- שאלת שם אופציונלית.
- הודעת סיום.
- הודעות המשך.
- שאלות בחירה.
- הגדרות מענה אנושי.
- הגדרת "אם לא ענו לשאלות".

הלינק שנוצר הוא לינק WhatsApp:

```text
https://wa.me/{phone}?text={trigger}
```

המשתמש לוחץ על הלינק, WhatsApp נפתח עם משפט הטריגר, והמשתמש שולח אותו.

## זרימת שיחה

הזרימה הכללית:

1. משתמש שולח משפט טריגר.
2. המערכת מזהה קמפיין פעיל.
3. המערכת רושמת התחלה של קמפיין.
4. אם מוגדרת שאלת שם:
   - נשאלת שאלת שם.
   - אם המשתמש עונה, השם נשמר.
   - אם לא עונה בזמן, נשמר שם ברירת מחדל.
5. המערכת שולחת הודעת סיום / הודעת פתיחה לפי הגדרת הקמפיין.
6. המערכת שולחת הודעות המשך אם הוגדרו.
7. המערכת מתחילה שאלות בחירה אם הוגדרו.
8. לפי תשובת המשתמש:
   - עוברים לשאלה הבאה.
   - שולחים קובץ.
   - שולחים טקסט מלווה.
   - מסיימים את התהליך.
   - מפנים לנציג אנושי אם התשובה לא קשורה.

## שאלות בחירה

שאלת בחירה יכולה להיות מוצגת כ:

- כפתורים.
- רשימה נפתחת.
- טקסט ממוספר.

ב-Twilio, לחיצה על כפתור יכולה להחזיר מזהה פנימי של תשובה, לא רק את טקסט התשובה. לכן `messageFlow.ts` מזהה תשובה לפי:

- מספר התשובה (`1`, `2`, `3`).
- מזהה התשובה.
- טקסט התשובה.

זה חשוב במיוחד כדי שהאפשרות "מעבר לשאלה הבאה" תעבוד בקמפיין Twilio.

## מעבר לשאלה הבאה

בתשובות אפשריות יש פעולה:

```text
מעבר לשאלה הבאה
```

התנהגות:

- אם נבחרה שאלה ספציפית, עוברים אליה.
- אם לא נבחרה שאלה ספציפית, המערכת מנסה לחבר אוטומטית לשאלה הבאה לפי הסדר.
- אם אין שאלה נוספת, השיחה מסתיימת.

יש גם אפשרות להגדיר טקסט מלווה לפני המעבר לשאלה הבאה, בדומה להודעת סיום.

## אם לא ענו לשאלות

הגדרה זו היא כללית לכל הקמפיין, ולא נמצאת בתוך כל שאלה בנפרד.

היא כוללת:

- כמה דקות לחכות לתשובה.
- איזו הודעה לשלוח אם לא התקבלה תשובה.

הערכים נשמרים בתוך:

```text
conversation.decisionTimeoutMinutes
conversation.decisionTimeoutText
```

## מענה אנושי / שאלה שלא קשורה לקמפיין

בבניית קמפיין Twilio יש אפשרות:

```text
מענה אוטומטי כשמבקשים שיחה אנושית
```

נוסח ברירת מחדל:

```text
אני מענה אוטומטי.
לשאלות נוספות אפשר לעבור לשיחה אנושית כאן:
[מעבר ל-WhatsApp]
```

יש גם שדה למספר WhatsApp אנושי.

בזמן שליחה, `[מעבר ל-WhatsApp]` מוחלף בקישור:

```text
https://wa.me/{phone}
```

נרמול מספרים:

- `0501234567` יהפוך ל-`972501234567`.
- `+972501234567` יהפוך ל-`972501234567`.
- `00972501234567` יהפוך ל-`972501234567`.

התנהגות:

- אם המשתמש עונה משהו שלא תואם אפשרות קיימת בזמן שאלת בחירה, נשלחת הודעת המענה האנושי.
- אחרי סיום הקמפיין, נשאר חלון handoff פתוח ל-24 שעות.
- אם המשתמש שולח שאלה חופשית בזמן החלון הזה, נשלחת שוב הודעת המעבר לנציג.
- האירוע נרשם כ-`human_handoff` כדי שיופיע בסטטיסטיקות.

## קבצים

קבצים הם ברמת לקוחה ולא ברמת פרויקט/שאלה בלבד.

בדשבורד הלקוחה יש אזור להעלאת קבצים כגון:

- קטלוג.
- תמונות מוצרים.
- PDF.
- וידאו קצר.

בשלב שאלות הבחירה אפשר לבחור קובץ שכבר הועלה ולשלוח אותו כתוצאה מבחירה.

מגבלת העלאה נוכחית:

```text
15MB
```

סוגים נתמכים:

- PDF
- JPG
- PNG
- WEBP
- MP4

## סטטיסטיקות תפעוליות ל-Twilio

ללקוחות Twilio מוצג מידע תפעולי במקום יומן טכני.

המשפך מציג:

- התחילו שיחה.
- קיבלו הודעה / קובץ.
- ענו לשאלה.
- הגיעו לסיום.
- עברו לנציג.
- נשמרו כאיש קשר.

נוסף משפט פשוט בסגנון:

```text
מתוך 120 אנשים שהתחילו, 37 הגיעו לסיום ו-9 ביקשו שיחה אנושית.
```

יומן הפעולות הפשוט מציג שורות כמו:

- הקמפיין הופעל.
- נשלחו 43 הודעות אוטומטיות.
- 5 אנשים ביקשו נציג.
- 2 שמירות נכשלו.

האירועים נרשמים דרך `recordCampaignEvent` ומשמשים לסטטיסטיקות.

## יומן הודעות Twilio

בדשבורד הלקוחה יש אזור ניהול עבור קמפיינים Twilio שבו ניתן לראות אילו הודעות נשלחו/נכנסו.

זה לא מיועד להיות לוג טכני מלא של Twilio, אלא כלי תפעולי:

- הודעות נכנסות.
- הודעות יוצאות.
- הודעות שנכשלו.
- הודעות שנדחו או התעלמו מהן.

הקוד המרכזי:

```text
src/twilioEvents.ts
```

## פרטים נדרשים ל-Twilio

בדשבורד הלקוחה יש אזור נפתח:

```text
פרטים נדרשים
```

האזור לא פתוח כברירת מחדל.

הוא אמור להסביר בפשטות מה צריך כדי להקים מספר Twilio:

- שם שיופיע ב-WhatsApp Business.
- מספר טלפון ייעודי או בקשה לקבל מספר מטוויליו.
- תחום פעילות.
- תיאור קצר של הפעילות.
- כתובת מייל עסקית אם קיימת.
- פרטי איש קשר לאישור.
- חומרים כגון קטלוג, תמונות מוצר או מסמך הסבר.

השפה צריכה להיות כללית ולא לפנות רק בלשון נקבה, גם אם רוב הלקוחות הן נשים.

אין לכתוב משפטים שמסבירים מה הלקוחה לא צריכה להבין. עדיף להסביר מה כן נדרש ומה המערכת עושה.

## Google Contacts

המערכת יכולה לשמור אנשי קשר ב-Google Contacts.

קבצים רלוונטיים:

- `src/googleContacts.ts`
- `credentials.json`
- `data/google-token.json`

אם Google מחובר, אנשי קשר נשמרים אוטומטית.

אם לא, הם נכנסים לתור שמירה או נרשמים מקומית לפי הגדרות המערכת.

## תור שמירת אנשי קשר

המערכת שומרת אנשי קשר דרך תור, כדי למנוע נפילה של כל השיחה בגלל תקלה זמנית ב-Google.

סטטוסים:

- `pending`
- `saved`
- `failed`

בדשבורד מוצגים:

- ממתינים לשמירה.
- נשמרו.
- נכשלו.

## Webhook Twilio

כניסת הודעות Twilio:

```text
POST /webhooks/twilio/whatsapp?token={TWILIO_WEBHOOK_TOKEN}
```

השרת בודק:

1. האם token נכון.
2. האם חתימת Twilio תקינה, אם `TWILIO_REQUIRE_SIGNATURE=true`.
3. האם היחידה מוגדרת כ-`TWILIO_API`.
4. האם יש `From`.
5. מה גוף ההודעה:
   - `ButtonPayload`
   - `ListId`
   - `ButtonText`
   - `Body`

לאחר מכן ההודעה מועברת אל:

```text
handleIncomingWhatsAppMessage
```

## משתני סביבה חשובים

משתנים כלליים:

| משתנה | תפקיד |
| --- | --- |
| `PORT` | פורט שרת |
| `CLIENT_ACCESS_TOKEN` | סיסמת כניסה ללקוחה |
| `OWNER_ACCESS_TOKEN` | טוקן בעלים ל-API פנימי |
| `CLIENT_PLAN` | מסלול לקוחה |
| `CLIENT_READONLY_DASHBOARD` | האם הדשבורד לקריאה בלבד |
| `CLIENT_MAX_CAMPAIGNS` | מגבלת קמפיינים |
| `CLIENT_SERVICE_EXPIRES_AT` | תוקף שירות |
| `WHATSAPP_PROVIDER` | `WEB_JS`, `BAILEYS`, או `TWILIO_API` |
| `BOT_REPLY_DELAY_MS` | דיליי בין הודעות |

משתני Twilio:

| משתנה | תפקיד |
| --- | --- |
| `TWILIO_ACCOUNT_SID` | Account SID |
| `TWILIO_AUTH_TOKEN` | Auth Token |
| `TWILIO_FROM` | מספר WhatsApp של הלקוחה |
| `TWILIO_MESSAGING_SERVICE_SID` | אופציונלי |
| `TWILIO_WEBHOOK_TOKEN` | token להגנת Webhook |
| `TWILIO_REQUIRE_SIGNATURE` | בדיקת חתימת Twilio |
| `TWILIO_QUICK_REPLY_CONTENT_SID` | תבנית כפתורים, אם קיימת |
| `TWILIO_LIST_PICKER_CONTENT_SID` | תבנית רשימה, אם קיימת |
| `TWILIO_MEDIA_BASE_URL` | בסיס URL לקבצי מדיה |

משתני Dokploy לבעלים:

| משתנה | תפקיד |
| --- | --- |
| `DOKPLOY_API_TOKEN` | טוקן API |
| `DOKPLOY_ENVIRONMENT_ID` | Environment יעד |
| `DOKPLOY_GIT_URL` | כתובת Git |
| `DOKPLOY_GIT_BRANCH` | ענף לפריסה |
| `DOKPLOY_CLIENT_DOMAIN_SUFFIX` | סיומת דומיין ללקוחות |
| `DOKPLOY_TWILIO_ACCOUNT_SID` | Twilio SID שיוזרק ללקוחות Twilio |
| `DOKPLOY_TWILIO_AUTH_TOKEN` | Twilio Auth Token שיוזרק |
| `DOKPLOY_TWILIO_FROM` | מספר ברירת מחדל, אם אין מספר ספציפי ללקוחה |
| `DOKPLOY_TWILIO_WEBHOOK_TOKEN` | token שיוזרק ללקוחות Twilio |
| `DOKPLOY_BOT_REPLY_DELAY_MS` | דיליי ברירת מחדל ללקוחות |

## תפעול קמפיין Twilio בפועל

תהליך מומלץ:

1. לקוח סוגר קמפיין.
2. שואלים האם יש לו מספר קיים או שהוא רוצה מספר חדש.
3. אם הוא רוצה מספר חדש:
   - קונים מספר ב-Twilio.
   - מחברים אותו כ-WhatsApp Sender.
   - מסיימים רישום ב-Meta/Twilio.
4. בדשבורד בעלים יוצרים או פותחים את הלקוחה.
5. מגדירים במסלול Twilio:
   - מספר Twilio.
   - דיליי הודעות.
6. מעתיקים את ה-Webhook המלא מהדשבורד.
7. מדביקים אותו ב-Twilio תחת Messaging Endpoint Configuration.
8. בונים קמפיין.
9. בודקים לינק `wa.me`.
10. שולחים הודעת טריגר ומוודאים:
    - הודעה ראשונה נשלחת.
    - שאלה 1 עובדת.
    - מעבר לשאלה 2 עובד.
    - מענה אנושי עובד.
    - סטטיסטיקות מתעדכנות.

## נקודות זהירות

- לא להחזיר או למחוק קבצים שלא קשורים למשימה.
- יש קבצים לא מנוהלים בסביבת העבודה שיכולים להיות טיוטות או קבצים זמניים.
- לפני קומיט, לבדוק `git status --short`.
- לא לעשות `git reset --hard`.
- אחרי שינוי TypeScript להריץ:

```bash
npm.cmd run build
```

## דברים שכדאי לשפר בעתיד

רעיונות שכבר עלו ונשמרו גם במסמך:

```text
docs/campaign-upgrade-roadmap.md
```

כיוונים מרכזיים:

- תבניות קמפיין מוכנות.
- Wizard קצר יותר לבניית קמפיין.
- בדיקת קמפיין לפני פרסום.
- ניהול מספרי Twilio בצורה מלאה מתוך הדשבורד.
- סטטיסטיקות עסקיות מפורטות יותר.
- סוגי קמפיינים נוספים מעבר לקמפיין טריגר.
- מעקב אחרי תקלות שמירת אנשי קשר בצורה ברורה יותר.

## מצב אחרון חשוב

השדרוגים האחרונים שנכנסו:

- דיליי הודעות מנוהל בדשבורד בעלים ונכנס ל-Environment של הלקוחה.
- Webhook מלא ל-Twilio מוצג להעתקה עם token.
- קמפיין Twilio מזהה לחיצות כפתורים לפי מזהה, טקסט או מספר.
- "מעבר לשאלה הבאה" עובד גם אם לא נבחר יעד מפורש.
- כפתור עריכה בקמפיין תוקן.
- מענה אנושי נשלח גם אחרי סיום הקמפיין, במשך חלון של 24 שעות.
- סטטיסטיקות משפך ויומן פעולות פשוט קיימים בדשבורד.

## Cost-saving default workflow

Default behavior for this project: work frugally with usage/time budget.

- Do not run `git commit` or `git push` unless explicitly asked. When relevant, provide the exact commands for the user to run instead.
- Prefer targeted file reads over opening large files fully.
- For large files, first use search, relevant line ranges, or file tails before reading the whole file.
- Avoid printing long tool outputs. Summarize only the important lines/results.
- Run tests/builds only when they are needed for a code change or explicitly requested.
- Avoid repeated verification once there is enough evidence.
- Before broad work, give a short estimate of scope/cost and wait if the user wants a cheaper path.
- If the task is only a question, answer from current context/knowledge without opening files unless essential.
- If a file must be changed, make the smallest focused edit and avoid scanning the whole project.
- Prefer giving concise status updates and final summaries.

